name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  MIX_TARGET: host
  ELIXIR_VERSION: "1.19.5"
  OTP_VERSION: "28.3"
  # System packages required to compile NIFs that mix deps.get pulls in
  # (libmnl-dev for vintage_net, libssl-dev for crypto-using NIFs).
  # libasound2-dev is needed by the sendspin_player Mix compiler, which
  # builds c_src/sendspin_player/ as part of `mix compile`.
  APT_PACKAGES: build-essential automake autoconf pkg-config libssl-dev libmnl-dev libasound2-dev

jobs:
  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: deps
          key: ${{ runner.os }}-host-deps-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-deps-
      - run: mix deps.get
      - run: mix format --check-formatted

  compile:
    name: Compile (warnings as errors)
    runs-on: ubuntu-latest
    env:
      MIX_ENV: dev
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: |
            deps
            _build/host_dev
          key: ${{ runner.os }}-host-dev-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-dev-
      - run: mix deps.get
      - run: mix compile --warnings-as-errors

  credo:
    name: Credo
    runs-on: ubuntu-latest
    env:
      MIX_ENV: dev
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: |
            deps
            _build/host_dev
          key: ${{ runner.os }}-host-dev-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-dev-
      - run: mix deps.get
      - run: mix credo --strict

  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      MIX_ENV: test
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: |
            deps
            _build/host_test
          key: ${{ runner.os }}-host-test-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-test-
      - run: mix deps.get
      - run: mix test --warnings-as-errors

  dialyzer:
    name: Dialyzer
    runs-on: ubuntu-latest
    env:
      MIX_ENV: dev
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: |
            deps
            _build/host_dev
          key: ${{ runner.os }}-host-dev-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-dev-
      - uses: actions/cache@v5
        with:
          path: priv/plts
          # Include the vendored path-dep sources (deps_local, e.g. the rebus
          # submodule) in the key: a path dep doesn't change mix.lock when it
          # changes, and dialyxir won't refresh a restored PLT for a same-version
          # path dep — a stale PLT then reports its new exports as a "missing
          # function". Hash .ex plus mix.exs (dep list / compiler opts) and
          # .erl/.hrl so a behavior/type change in any vendored source busts it.
          # The same hash is in the restore-keys prefix so a vendored-dep change
          # can't fall back to a stale PLT (forces a fresh build); a mix.lock-only
          # change still reuses the PLT.
          key: ${{ runner.os }}-plt-${{ env.ELIXIR_VERSION }}-${{ env.OTP_VERSION }}-${{ hashFiles('deps_local/**/*.ex', 'deps_local/**/mix.exs', 'deps_local/**/*.erl', 'deps_local/**/*.hrl') }}-${{ hashFiles('mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-plt-${{ env.ELIXIR_VERSION }}-${{ env.OTP_VERSION }}-${{ hashFiles('deps_local/**/*.ex', 'deps_local/**/mix.exs', 'deps_local/**/*.erl', 'deps_local/**/*.hrl') }}-
      - run: mix deps.get
      - run: mix dialyzer --format github

  hex-audit:
    name: Hex audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # rebus is an all-target path dep vendored as a git submodule
          # (deps_local/rebus); without it `mix deps.get`/compile fail.
          submodules: recursive
      - run: sudo apt-get update && sudo apt-get install -y ${{ env.APT_PACKAGES }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_VERSION }}
          otp-version: ${{ env.OTP_VERSION }}
      - run: mix archive.install hex nerves_bootstrap --force
      - uses: actions/cache@v5
        with:
          path: deps
          key: ${{ runner.os }}-host-deps-${{ hashFiles('mix.lock') }}
          restore-keys: ${{ runner.os }}-host-deps-
      - run: mix deps.get
      # `mix hex.audit` has no per-package ignore flag. cowlib 2.18.0 (a
      # transitive dep via cowboy) has two advisories with no fixed release
      # available upstream; allow ONLY those specific IDs so the gate still
      # fails on any other advisory (including a future req regression).
      # Remove this allow-list once cowlib ships a patched release.
      - name: Hex audit (cowlib 2.18.0 advisories allow-listed — no upstream fix)
        run: |
          ALLOWED='EEF-CVE-2026-43966|EEF-CVE-2026-43969'
          audit=$(mix hex.audit) && exit 0
          echo "$audit"
          unexpected=$(echo "$audit" | grep -E '^  [^ ]+ [0-9]' | grep -Ev " - (${ALLOWED}) " || true)
          if [ -n "$unexpected" ]; then
            echo "::error::hex.audit found non-allow-listed advisories:"
            echo "$unexpected"
            exit 1
          fi
          echo "Only allow-listed cowlib advisories present; passing."
      - run: mix deps.unlock --check-unused
