# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where third-party dependencies like ExDoc output generated docs.
/doc/

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Temporary files, for example, from tests.
/tmp/

# Nerves artifacts and firmware files
/.nerves/
*.fw

# ElixirLS and other LSP workspace files
/.elixir_ls/
/.lexical/

# Local mise overrides (keep .mise.toml committed)
.mise.local.toml

# Environment files with secrets
.env
.env.local
.env.*.local

# Built assets (generated by mix assets.deploy / mix phx.digest)
/priv/static/assets/
/priv/static/cache_manifest.json
# Digested/gzipped copies of static files
/priv/static/**/*-*.gz
/priv/static/**/*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f]*.*

# OS generated files
.DS_Store
Thumbs.db
